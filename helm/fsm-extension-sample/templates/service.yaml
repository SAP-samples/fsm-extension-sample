apiVersion: v1
kind: Service
metadata:
  name: {{ include "fsm-extension-sample.fullname" . }}
  labels:
    {{- include "fsm-extension-sample.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fsm-extension-sample.selectorLabels" . | nindent 4 }}
