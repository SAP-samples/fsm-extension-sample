apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fsm-extension-sample.fullname" . }}-test-connection"
  labels:
    {{- include "fsm-extension-sample.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fsm-extension-sample.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
